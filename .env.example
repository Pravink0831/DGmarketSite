# =====================================================================
# DGmarket · Environment variable template
# Copy to `.env` (git-ignored). Only VITE_* vars reach client code.
# Never place server secrets here — the client bundle is public.
# =====================================================================
VITE_SITE_TITLE="DGmarket — Web Design Studio"
VITE_SITE_URL="https://your-domain.example"
VITE_ANALYTICS_ID=""
VITE_CONTACT_ENDPOINT=""
